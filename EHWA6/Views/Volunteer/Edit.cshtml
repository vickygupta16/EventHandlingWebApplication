@model EHWA6.Models.Volunteer

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Volunteer</legend>

        @Html.HiddenFor(model => model.VolunteerId)

        <div class="editor-label">
            @Html.LabelFor(model => model.EventsId, "evs")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EventsId", String.Empty)
            @Html.ValidationMessageFor(model => model.EventsId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId, "ups")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
